---
# 全局邊界規則

以下為可直接放入 `AGENTS.md` 或架構規範文件的**模組邊界規則句**，涵蓋：

- account
- capabilitys（建議未來改為 capabilities）
- eventing
- integration
- shared
- shell
- workflows
- workspace

語句採條文化，可直接作為團隊約束。
---

# 📐 全域邊界總則

- 僅允許透過各模組 `index.ts` 的 public API 進行引用。
- 禁止 deep import 任一模組內部資料夾。
- 禁止循環依賴。
- 禁止跨模組直接存取 infrastructure 層。
- 應優先透過 application service、facade 或 event 溝通。
- shared 為最低層，可被依賴，但不得依賴業務模組。

---

# 🧱 依賴方向總圖（文字版）

```

shell
├─ capabilitys
├─ workspace
├─ account
├─ workflows
│
├─ integration
├─ eventing
│
└─ shared   （最低層）

```

---

# 🧩 shared 邊界規則

- shared 僅包含通用模型、基礎型別、工具與抽象。
- shared 不得依賴任何業務模組。
- shared 不得引用 Angular feature module。
- shared 不得包含業務語意。

規則句：

> shared 是基礎層，只能被依賴，不得依賴他人。

---

# 👤 account 邊界規則

- account 負責身份、使用者與認證上下文。
- 可被 workspace、capabilitys、shell 依賴。
- 不得依賴 capabilitys。
- 不得包含 UI layout 邏輯。
- 對外只暴露 identity 與 auth 相關 service / model。

規則句：

> account 提供身份上下文，不承載業務能力。

---

# 🏢 workspace 邊界規則

- workspace 定義工作空間上下文與範圍。
- capabilitys 可依賴 workspace。
- workspace 不得依賴任何 capability。
- workspace 不得包含具體業務流程。
- workspace 僅提供 context、scope、resolver、policy。

規則句：

> workspace 提供範圍上下文，不實作能力行為。

---

# ⚙️ capabilitys 邊界規則

- 每個子資料夾為獨立業務能力模組。
- capability 與 capability 之間不得直接依賴。
- capability 可依賴：
  - shared
  - workspace
  - account
  - eventing（發送事件）
- capability 不得依賴其他 capability 的內部實作。
- 跨能力互動必須透過 event 或 facade。

規則句：

> capability 彼此隔離，只能透過事件或公開 facade 協作。

---

# 🔄 workflows 邊界規則

- workflows 負責跨能力流程編排。
- workflows 可依賴多個 capability 的 public API。
- capability 不得反向依賴 workflows。
- workflows 不得包含 UI 元件。
- workflows 只做 orchestration，不持有 UI state。

規則句：

> workflows 可以組合能力，但能力不可反向依賴流程。

---

# 📡 eventing 邊界規則

- eventing 定義事件模型與事件匯流機制。
- 所有模組可依賴 eventing。
- eventing 不得依賴業務模組。
- event payload 必須為 DTO 或 event model。
- 不得在 eventing 中放業務邏輯。

規則句：

> eventing 提供事件機制，不承載業務決策。

---

# 🔌 integration 邊界規則

- integration 負責外部系統與遠端資源整合。
- integration 可依賴 shared 與 eventing。
- 業務模組不得直接依賴 integration adapter。
- 必須透過 interface 或 facade 存取整合能力。
- integration 不得包含 UI 或 route。

規則句：

> integration 隔離外部系統，只透過介面向內提供能力。

---

# 🖥 shell 邊界規則

- shell 為 UI 組裝層與路由入口。
- shell 可依賴所有 feature 模組。
- 其他模組不得依賴 shell。
- shell 不得包含 domain 規則。
- shell 只負責 layout、route、composition。

規則句：

> shell 負責組裝，不負責業務。

---

# 🔒 Import 規則句（可單獨列出）

> 僅允許從模組根匯入 public API；禁止引用任何 `/domain`、`/application`、`/infrastructure` 內部路徑。

---

# 🧪 測試隔離規則

- 每個模組測試需可獨立運行。
- 測試不得跨模組載入 infrastructure。
- capability 測試應 mock workspace / account context。

---
