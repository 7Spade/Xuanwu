---

# Workspace ↔ Capabilities 邊界規則

## 🎯 定位規則

- `workspace` 是「工作空間上下文模組」，負責工作空間狀態、切換、上下文解析與範圍控制。
- `capabilities` 是「業務能力模組集合」，每個子模組代表一個可獨立演進的業務能力。
- `workspace` 屬於上層上下文提供者，`capabilities` 屬於能力消費者。

---

# 📦 依賴方向規則

## 允許

- capabilities → workspace ✅（只允許透過 public API）
- capabilities → shared ✅
- capabilities → account / permissions（若屬於安全上下文）✅

## 禁止

- workspace → capabilities ❌
- capability A → capability B 直接引用 ❌
- capability → 另一 capability 的 internal 檔案 ❌

---

# 🔒 Import 邊界規則

## 只允許 public surface

允許：

```ts
import { WorkspaceContext } from '@app/workspace';
```

````

禁止：

```ts
import { WorkspaceContext } from '@app/workspace/domain/context';
import { XxxService } from '@app/workspace/infrastructure/...';
```

規則句：

> 僅允許透過 index.ts 對外匯出的 public API 進行引用，禁止 deep import。

---

# 🧭 Workspace 職責邊界

workspace 只可以負責：

- 當前 workspace id / scope
- workspace config
- workspace members context
- workspace lifecycle
- workspace-level policies

workspace 不可以負責：

- 任一具體業務流程
- 任一 capability 的業務規則
- 任一 capability 的 UI 行為

---

# 🧩 Capability 職責邊界

每個 capability：

- 必須可在不同 workspace 下運作
- 不得自行決定 workspace 選擇邏輯
- 不得修改 workspace 狀態
- 僅可透過 workspace application service 讀取上下文

規則句：

> Capability 是 workspace-aware，但不是 workspace-owner。

---

# 🔁 溝通方式規則

capabilities 與 workspace 溝通只允許：

- application service
- facade
- query interface
- event bus

禁止：

- 直接存取 store / signal / subject
- 直接呼叫 infrastructure adapter

---

# 🧨 變更隔離規則

- workspace schema 變更不得破壞 capability 編譯
- capability schema 變更不得要求 workspace 修改 domain model
- 跨模組資料結構必須經過 DTO 或 query model

---

# 🧪 測試邊界規則

- capability 測試需可 mock workspace context
- workspace 測試不得載入任何 capability module
- 不允許跨 capability integration test（應透過 event / facade）

---

# 🧱 路由邊界規則（若使用 Angular route）

- workspace 提供 route scope
- capabilities 作為 lazy feature route
- capability route 不得反向控制 workspace route

---

# 📜 一句總規範（可放最上面）

> Workspace 定義上下文與範圍；Capabilities 實作業務能力。
> 依賴只能由 Capability 指向 Workspace，且僅限 public API。
> 禁止反向依賴、禁止跨 Capability 直接引用、禁止 deep import。

---
````
