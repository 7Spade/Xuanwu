{
  "postToolUse": [
    {
      "name": "Format and Lint affected file",
      "description": "Ensures the code generated by Copilot matches project linting and formatting rules.",
      "command": "npx eslint --fix \"$TOOL_OUTPUT_FILE_PATH\" && npx prettier --write \"$TOOL_OUTPUT_FILE_PATH\"",
      "condition": "$TOOL_OUTPUT_FILE_PATH =~ /\\.(ts|tsx|js|jsx)$/"
    }
  ]
}