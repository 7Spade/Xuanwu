/**
 * Module: index.ts
 * Purpose: Barrel exports for shared ontology vocabularies.
 * Responsibilities: expose canonical ontology constants for cross-slice usage.
 * Constraints: deterministic logic, respect module boundaries
 */

export { SEARCH_DOMAINS, TAXONOMY_DIMENSIONS } from './semantic-taxonomy';
