/**
 * src/shared/infra/storage/index.ts
 *
 * Public API for the shared infrastructure storage module.
 *
 * Exports the DDD-layer StorageAdapter that implements the IFileStore port.
 */

export { StorageAdapter, storageAdapter } from './storage.adapter';
