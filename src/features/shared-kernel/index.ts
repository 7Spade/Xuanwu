/**
 * Module: index.ts
 * Purpose: keep backward compatibility for legacy imports
 * Responsibilities: re-export canonical shared-kernel API
 * Constraints: deterministic logic, respect module boundaries
 */

export * from '@/shared-kernel';
