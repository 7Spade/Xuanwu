/**
 * Module: _semantic-authority.ts
 * Purpose: Backward-compatible shim for semantic authority constants.
 * Responsibilities: re-export canonical ontology vocabularies from shared-kernel.
 * Constraints: deterministic logic, respect module boundaries
 */

export { SEARCH_DOMAINS, TAXONOMY_DIMENSIONS } from '@/shared-kernel';
